apiVersion: v1
kind: Service
metadata:
  name: smartappointment-service
spec:
  selector:
    app: smartappointment
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
